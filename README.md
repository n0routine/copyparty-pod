# Copyparty Podman config

Minimal locked-down rootless Podman container for running Copyparty.

### Configuration

Create a `copyparty-account.conf` for setting user accounts and share folders.

Use `image.sh` to create and format a sparse ext4 image `disk.img` for file storage.

Run `service.sh` to build and launch the container.
